import express from 'express'; import multer from 'multer'; import fs from 'fs'; import path from 'path'; import { validatePortfolioData, calculateMetrics, calculateRiskMetrics, calculateAllocation, sectorPerformance, topHoldings } from '../utils/analytics.js'; const router=express.Router(); const upload=multer({dest:'uploads/'}); function parseCsv(text){const [header,...lines]=text.trim().split(/\r?\n/); const cols=header.split(',').map(c=>c.trim().toLowerCase()); return lines.filter(Boolean).map(line=>{const vals=line.split(','); const row={}; cols.forEach((c,i)=>row[c]=vals[i]); return row;});} function shapeResponse(rows,validation,benchmarkReturn=4.2,peer1=3.8,peer2=4.0){const metrics=calculateMetrics(rows); const riskMetrics=calculateRiskMetrics(rows); const allocation=calculateAllocation(rows); const sectorPerf=sectorPerformance(rows); const benchmarkData={benchmark_return:Number(benchmarkReturn),alpha:metrics.avg_return-Number(benchmarkReturn)}; const peerAverage=(Number(peer1)+Number(peer2))/2; const peerData={peer_1_return:Number(peer1),peer_2_return:Number(peer2),peer_average:peerAverage,relative_vs_peers:metrics.avg_return-peerAverage}; return {validation,metrics,riskMetrics,allocation,sectorPerformance:sectorPerf,benchmarkData,peerData,topHoldings:topHoldings(rows),rows}; } router.get('/sample',(_,res)=>{const csv=fs.readFileSync(path.join('sample','sample_fund.csv'),'utf-8'); const parsed=parseCsv(csv); const {messages,cleaned}=validatePortfolioData(parsed); res.json(shapeResponse(cleaned,messages));}); router.post('/upload',upload.fields([{name:'portfolio',maxCount:1},{name:'logo',maxCount:1}]),(req,res)=>{const file=req.files?.portfolio?.[0]; if(!file) return res.status(400).json({error:'Portfolio CSV is required.'}); const csv=fs.readFileSync(file.path,'utf-8'); const parsed=parseCsv(csv); const {messages,cleaned}=validatePortfolioData(parsed); res.json(shapeResponse(cleaned,messages,req.body.benchmarkReturn,req.body.peer1,req.body.peer2));}); export default router;